# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール
- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 「実装が複雑すぎるため後回し」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

### 実装可能なタスクのみを計画
- 計画段階で「実装可能なタスク」のみをリストアップ
- 「将来やるかもしれないタスク」は含めない
- 「検討中のタスク」は含めない

### タスクスキップが許可される唯一のケース
以下の技術的理由に該当する場合のみスキップ可能:
- 実装方針の変更により、機能自体が不要になった
- アーキテクチャ変更により、別の実装方法に置き換わった
- 依存関係の変更により、タスクが実行不可能になった

スキップ時は必ず理由を明記:
```markdown
- [x] ~~タスク名~~（実装方針変更により不要: 具体的な技術的理由）
```

### タスクが大きすぎる場合
- タスクを小さなサブタスクに分割
- 分割したサブタスクをこのファイルに追加
- サブタスクを1つずつ完了させる

## 🚨 TDD必須の原則

**実装タスクは必ずテスト駆動開発（TDD）で行うこと**（詳細: `.claude/guides/tdd.md`）

- 実装タスクには必ず RED / GREEN / REFACTOR のサブタスクを含める
- テストコードを書く前に実装コードを書くことは禁止
- REDの失敗確認・GREENの成功確認は必ず `uv run pytest` を実行して行う
- REFACTORサブタスク: 改善点がなければリファクタリング自体は不要（その場合はそのまま`[x]`にしてよい）。ただしリファクタリングを行った場合は全テストの再実行・成功確認を省略しない

---

## フェーズ1: {フェーズ名}

- [ ] {実装対象1}を実装
  - [ ] RED: `tests/{対応パス}/test_{名前}.py` にテストを作成し、実行して失敗を確認
  - [ ] GREEN: `src/{対応パス}/{名前}.py` を実装し、テスト成功を確認
  - [ ] REFACTOR: リファクタリングと全テスト成功確認

- [ ] {実装対象2}を実装
  - [ ] RED: テストを作成し、実行して失敗を確認
  - [ ] GREEN: 実装し、テスト成功を確認
  - [ ] REFACTOR: リファクタリングと全テスト成功確認

## フェーズ2: {フェーズ名}

- [ ] {実装対象3}を実装
  - [ ] RED: テストを作成し、実行して失敗を確認
  - [ ] GREEN: 実装し、テスト成功を確認
  - [ ] REFACTOR: リファクタリングと全テスト成功確認

## フェーズ3: 品質チェックと修正

- [ ] すべてのテストが通ることを確認
  - [ ] `uv run pytest`
- [ ] カバレッジが80%以上であることを確認
  - [ ] `uv run pytest --cov=src`
- [ ] リントエラーがないことを確認
  - [ ] `uv run ruff check .`
- [ ] フォーマットが正しいことを確認
  - [ ] `uv run ruff format --check .`

## フェーズ4: ドキュメント更新

- [ ] README.md を更新（必要に応じて）
- [ ] 実装後の振り返り（このファイルの下部に記録）

---

## コードレビュー結果

> 全タスク完了後、`implementation-validator`サブエージェントによるコードレビューの結果をここに記録する（steeringスキル モード2 ステップ5）。

### レビュー実施日
{YYYY-MM-DD}

### 指摘件数と対応状況

| 優先度 | 件数 | 対応状況 |
|--------|------|----------|
| [必須] | {X}件 | {全て修正済み（必須）} |
| [推奨] | {Y}件 | {Z件修正 / 未対応の理由: ...} |
| [提案] | {W}件 | {振り返りに記録} |

### 修正内容の要約
- {指摘内容と修正内容}

---

## 実装後の振り返り

### 実装完了日
{YYYY-MM-DD}

### 計画と実績の差分

**計画と異なった点**:
- {計画時には想定していなかった技術的な変更点}
- {実装方針の変更とその理由}

**新たに必要になったタスク**:
- {実装中に追加したタスク}
- {なぜ追加が必要だったか}

**技術的理由でスキップしたタスク**（該当する場合のみ）:
- {タスク名}
  - スキップ理由: {具体的な技術的理由}
  - 代替実装: {何に置き換わったか}

**⚠️ 注意**: 「時間の都合」「難しい」などの理由でスキップしたタスクはここに記載しないこと。全タスク完了が原則。

### 学んだこと

**技術的な学び**:
- {実装を通じて学んだ技術的な知見}
- {新しく使った技術やパターン}

**コードレビューからの学び**:
- {レビュー指摘から得た知見（詳細は「コードレビュー結果」セクション参照）}

**プロセス上の改善点**:
- {タスク管理で良かった点}
- {ステアリングファイルの活用方法}

### 次回への改善提案
- {次回の機能追加で気をつけること}
- {より効率的な実装方法}
- {タスク計画の改善点}
