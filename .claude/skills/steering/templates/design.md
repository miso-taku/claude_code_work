# 設計書

> **設計ルール**: この設計はドメイン駆動設計（DDD）に従うこと。詳細は `.claude/guides/ddd.md` を参照。
> 完成前に同ガイドの「設計時チェックリスト」で自己検証すること。

## アーキテクチャ概要

{採用するアーキテクチャパターンを記述。レイヤーは domain / application / infrastructure / presentation の4層構造に従う}

```
{アーキテクチャ図をテキストやMermaidで記述}
```

## ドメインモデル（DDD・必須）

### エンティティ・値オブジェクト・集約

| 名前 | 種別 | 所属する集約 | 責務・不変条件 |
|------|------|-------------|---------------|
| {名前} | エンティティ（集約ルート） | {集約名} | {守るべきビジネスルール} |
| {名前} | 値オブジェクト | {集約名} | {バリデーションルール} |

### 集約境界と参照ルール

- {集約A} → {集約B}: IDで参照（{xxx}_id）

### リポジトリ

| リポジトリ | 対象集約 | インターフェース定義場所 | 実装場所 |
|-----------|---------|----------------------|---------|
| {名前}Repository | {集約名} | `domain/repositories/` | `infrastructure/repositories/` |

### ユースケース（アプリケーションサービス）

| ユースケース | 概要 | 使用するドメインオブジェクト |
|-------------|------|---------------------------|
| {名前}UseCase | {概要} | {エンティティ・リポジトリ等} |

### DDDチェックリスト（design.md完成時に確認）

- [ ] 用語はglossary.md（ユビキタス言語）と一致している
- [ ] エンティティと値オブジェクトを区別した
- [ ] ビジネスルール・不変条件はドメイン層に配置した
- [ ] 集約境界を定義し、他の集約はID参照にした
- [ ] リポジトリはインターフェース（domain層）と実装（infrastructure層）を分離した
- [ ] 依存方向は presentation → application → domain ← infrastructure になっている

## コンポーネント設計

### 1. {コンポーネント名1}

**責務**:
- {責務1}
- {責務2}

**実装の要点**:
- {実装時の注意点}
- {技術的な制約}

### 2. {コンポーネント名2}

**責務**:
- {責務1}
- {責務2}

**実装の要点**:
- {実装時の注意点}
- {技術的な制約}

## データフロー

### {ユースケース名}
```
1. {ステップ1}
2. {ステップ2}
3. {ステップ3}
```

## エラーハンドリング戦略

### カスタムエラークラス

{必要なエラークラスを定義}

### エラーハンドリングパターン

{エラーをどのように処理するか}

## テスト戦略

> **実装ルール**: 実装はTDD（テスト駆動開発）で行う。テストを先に書き、Red→Green→Refactorのサイクルを厳守する。詳細は `.claude/guides/tdd.md` を参照。

### ユニットテスト（`tests/unit/`）
- {テスト対象1: 正常系・異常系・境界値}
- {テスト対象2: 正常系・異常系・境界値}

### 統合テスト（`tests/integration/`）
- {テストシナリオ1}
- {テストシナリオ2}

## 依存ライブラリ

{新しく追加するライブラリがあれば記述}

```bash
uv add {ライブラリ名}
uv add --dev {開発用ライブラリ名}
```

## ディレクトリ構造

```
{追加・変更されるファイル構造}
```

## 実装の順序

1. {実装ステップ1}
2. {実装ステップ2}
3. {実装ステップ3}

## セキュリティ考慮事項

- {セキュリティ上の注意点}

## パフォーマンス考慮事項

- {パフォーマンス上の注意点}

## 将来の拡張性

{将来的な拡張を考慮した設計}
